function PriorityQueue() {
    this.peek = function (){};
}

function testRound(flag) {
    var queue = new PriorityQueue();
    if(flag > 100){
    }
    queue.peek();
}

testRound(J$.readInput(1));
testRound(J$.readInput(1));